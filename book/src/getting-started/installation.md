# Installation

How to get a Rustapi server running — the Docker Compose quickstart for a one-command demo, and the cargo workflow for running the backend against your own Postgres. Covers prerequisites and the optional admin UI bundle. <!-- TODO: fill -->
