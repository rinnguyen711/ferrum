# Introduction

Rustapi is a headless CMS framework written in Rust (Axum + sqlx) with a React + TypeScript admin UI. This documentation covers installing Rustapi, the core concepts behind its content model, and task-oriented guides for building with it. For the auto-generated, schema-aware REST reference, see the live Swagger UI at `/docs` on a running server. <!-- TODO: fill -->
