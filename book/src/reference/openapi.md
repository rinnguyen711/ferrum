# OpenAPI / Swagger

A running server generates an OpenAPI 3.1 spec from the live content-type registry and serves a browsable Swagger UI. This page explains how to reach `/openapi.json` and `/docs` and when to disable them. <!-- TODO: fill -->
